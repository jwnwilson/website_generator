{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "bookshelf",
      "settings": {
        "client": "postgres",
        "database": "cms",
        "host": "localhost",
        "port": 5432,
        "username": "docker",
        "password": "docker"
      },
      "options": {}
    }
  }
}
