{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "deploy.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/deployment",
      "handler": "deploy.find",
      "config": {
        "policies": []
      }
    }
  ]
}
